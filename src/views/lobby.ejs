<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= lobby.name %> - Lockkeroom</title>
    <style>
        /* Add your CSS styles here */
    </style>
</head>
<body>
    <header>
        <h1><%= lobby.name %></h1>
        <p><%= lobby.description %></p>
    </header>
    <section>
        <h2>Messages</h2>
        <ul>
            <% messages.forEach(message => { %>
                <li><strong><%= message.author %>:</strong> <%= message.content %></li>
            <% }); %>
        </ul>
        <form action="/api/lobby/<%= lobby.id %>/messages" method="post">
            <textarea name="content" placeholder="Type your message..."></textarea>
            <button type="submit">Send</button>
        </form>
    </section>
    <footer>
        &copy; 2023 Lockkeroom. All rights reserved.
    </footer>
</body>
</html>
